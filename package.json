{
	"name": "nuxt-zixin",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "nuxt build",
		"dev": "nuxt dev",
		"generate": "nuxt generate",
		"devview": "npm run build && wrangler dev --local",
		"prodview": "npm run build && wrangler dev",
		"postinstall": "nuxt prepare",
		"deploy": "npm run build && wrangler deploy",
		"cf-typegen": "wrangler types"
	},
	"dependencies": {
		"@pinia/nuxt": "^0.11.3",
		"axios": "^1.13.4",
		"nuxt": "^4.2.2",
		"pinia": "^3.0.4",
		"pinia-plugin-persistedstate": "^4.7.1",
		"vue": "^3.5.27",
		"vue-router": "^4.6.4"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20260130.0",
		"@types/node": "^25.1.0",
		"nitro-cloudflare-dev": "^0.2.2",
		"nitropack": "^2.13.1",
		"wrangler": "^4.59.3"
	}
}
